{
  "name": "stratums-io-reborn",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Stratums.io server and client project",
  "license": "GNU",
  "scripts": {
    "build": "npm run build --workspace client",
    "build:dev": "npm run build:dev --workspace client",
    "start": "npm run start --workspace server",
    "dev": "npm run dev --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "puppeteer": "^24.29.1"
  }
}
